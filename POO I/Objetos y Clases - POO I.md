- Isomorfismo, que es?
- Que es un objeto?
<font color="#7f7f7f">Podemos ver y podemos modelar en base a lo que vemos</font>

- Que es una clase?:
<font color="#7f7f7f">Se identifican con una estructura interna y métodos asociados que definen que es</font>

- Cual es la diferencia entre Objetos y Clases?
<font color="#7f7f7f">Si hablamos de Objetos y Clases la diferencia abarca en que a un conjunto de animales lo vamos a representar en una clase, independientemente de que los animales sean de distinto "tipo" sea perro o gato, sigue siendo un animal, luego, cuando creamos un objeto le damos la identidad dependiendo de que tipo de animal es.</font>

- Clases e instancias:
<font color="#7f7f7f">El constructor nos permite crear (o inicializar) nuestras variables de instancias</font>
<font color="#7f7f7f">Luego siguen los metodos</font>

<font color="#7f7f7f">palabra reservada que se utiliza para crear una instancia (variable de instancia):</font> <font color="#4f81bd">new</font>
<font color="#7f7f7f">un ejemplo para esto seria:</font> Alumno a1 = <font color="#4f81bd">new</font> Alumno ("Mauro");


- La otra verdad de las clases
Hay 3 formas de conocimiento de o tipos de relaciones entre objetos
1. Conocimiento interno: variables de instancia
2. Conocimiento externo: parámetros
		-
3. Conocimiento temporal: variables temporales

 Nota: BUSCAR INFORMACION EXTERNA

- Creación de objetos
Para llamar a un objeto a si mismo, la palabra reservada es <font color="#4f81bd">this</font>
- Envío de mensajes
- Formas de conocimiento
- Terminología de objetos aplicadas a programas JAVA
- Colecciones

Ejemplo

Como pensar en una caja de ahorro para reutilizarla... 

Cuando identifico una clase es SIEMPRE en singular y no plural, <font color="#7f7f7f">por más cantidad de ese tipo de clase que veamos</font>
